// Determines the factor for the optimal viewport width
// depending on the percentage width of the section.
// For example, when the section width is set to 50%,
// the ideal viewport width for the whiteboard is 500px.
// Given the total page width is ~1000px, the whiteboard
// would then render in its natural size (unscaled).
export const OPTIMAL_VIEWPORT_WIDTH_FACTOR = 10;
